<div class="d-flex align-items-center justify-content-center">
  <h1> Home Page</h1>
</div>
<% if Current.user%>
  Logged in as <%= Current.user.email%>
  <%= link_to "Logout" ,logout_path(protocol: 'https'), method: :delete %>
  <%= button_to "Logout" ,logout_path, method: :delete  %>
<% end%>
